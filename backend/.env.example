# Backend secrets — replace these placeholders with your real values.
# This file IS committed so the structure is documented, but the values
# below are placeholders. Put your real secrets in `.env` (gitignored).
#
# IMPORTANT: never commit real API keys. If you accidentally did, rotate them.

GROQ_API_KEY=your-groq-api-key-here
EMAIL_ADDRESS=your-email@example.com
EMAIL_APP_PASSWORD=your-gmail-app-password-here